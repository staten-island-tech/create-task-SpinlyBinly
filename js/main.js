//3 seperate choose a number between 255 to create a random color value
//create buttons that show up as randomly generated colors
const array = [];
for (let i = 0; i <= 255; i++) {
  array.push(i);
}

console.log(array);
//math.random functin
let a = 0;
let b = 0;
let c = 0;

function random() {
  DOMSelectors.container.insertAdjacentHTML(`beforeend`, ``);
}
random();
